# Uncomment the next line to define a global platform for your project
platform :osx, '10.13'

target 'MarvelForKeynote' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for KeynoteToMarvel
  pod 'SwiftyJSON'
  pod 'MarvelAuth', :git => 'git@github.com:marvelapp/oauth-demo-ios.git', :branch => 'master'

  target 'MarvelForKeynoteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MarvelForKeynoteUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
